extends layout

block content
  style.
    #map {
      width: 100%;
      height: 500px;
    }

  .page-header
    h1 404

  h4 The page you were you looking for either does not exist or requires admin privileges. 
